# 项目协作规则

## 版本快照

用户要求：每次代码改动或设计画布改动完成并验证后，都保存一个可回滚版本快照到本项目的 GitHub 仓库。

- 开始改动前先检查 `git status`、当前分支和远程仓库，保留用户已有的未提交改动。
- 一组逻辑完整、可单独验收的代码改动完成后，运行相应检查，更新 `VERSION_SNAPSHOTS.md`，创建一次 Git 提交并推送当前快照分支。
- 提交标题使用 `snapshot(code): <变更摘要>`、`snapshot(canvas): <变更摘要>` 或 `snapshot(full): <变更摘要>`。
- Figma 或 Penpot 画布有改动时，除画布自身版本外，还要在 `design-snapshots/` 保存快照清单；记录平台、文件链接、页面或节点、版本说明、截图或导出文件路径以及对应 Git commit。
- 画布二进制内容无法直接由 Git 回滚时，至少保存 PNG/PDF/SVG 导出或截图和结构化状态文件。恢复时以快照清单指向的画布版本或导出文件为准。
- 不重写、不压缩、不删除已经推送的快照提交，除非用户明确要求。
- 如果 GitHub 远程、凭据或网络不可用，仍创建本地提交并在回复中明确标记“待推送”；不得把本地提交表述为已保存到 GitHub。
- 未经用户明确授权，不把与当前任务无关的已有修改纳入快照。

