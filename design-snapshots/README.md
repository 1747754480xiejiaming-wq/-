# 设计画布快照

Figma 是主画布，Penpot 仅在 Figma 需要付费或额度耗尽时承接受阻部分。每次画布改动后，在此目录新增一个 JSON 清单，并保存相应截图或导出文件；再与代码一起提交到 GitHub。

文件命名：`YYYYMMDD-NNN-平台-范围.json`。清单至少包含：快照编号、平台、设计文件链接、页面或节点、变更摘要、导出物、对应代码基线和 Git commit。

