import {useState,useCallback} from 'react';
import {useApp,roleNames,statusNames,ItemStatus,TeaItem,goto,url,downloadCsv} from './model';
import {Icon,Button,Badge,StatusBadge,Notice,Empty,Modal} from './ui';

export function Overview(){const {items,leads,role,audit}=useApp();const published=items.filter(i=>i.status==='published').length,pending=items.filter(i=>(i.draftStatus||i.status)==='pending_review').length;return <><div className="admin-title"><div><div className="eyebrow">WORKSPACE / 工作概览</div><h1>把每一份茶资料，认真交付。</h1><p>下午好，{roleNames[role]}。这里是当前演示工作区的内容与线索概况。</p></div><Button href="/admin/imports" icon="upload" tone="secondary">导入资料</Button></div><div className="stats-grid">{[['已发布茶品',published,'首发范围内的公开档案','leaf'],['待审核内容',pending,'审核通过后仍需单独发布','file'],['咨询线索',leads.length,'仅演示数据，无真实发送','chat'],['待跟进线索',leads.filter(l=>l.status==='new').length,'主动同意后登记的需求','users']].map(([label,note,sub,ic])=><div className="stat-card" key={label}><div><span>{label}</span><Icon name={String(ic)}/></div><strong>{note}</strong><small>{sub}</small></div>)}</div><div className="admin-two-col"><section className="panel"><div className="section-head"><h2>今天先处理这些</h2><Badge tone="warning">{pending} 项待审核</Badge></div><div className="task-list">{items.filter(i=>(i.draftStatus||i.status)==='pending_review').map(t=><a href={url('/admin/review/'+t.id)} key={t.id}><span className="task-icon"><Icon name="file"/></span><div><strong>{t.name}</strong><p>{t.batch} · 等待内容审核</p></div><Icon name="arrow" size={18}/></a>)}<a href={url('/admin/leads')}><span className="task-icon"><Icon name="chat"/></span><div><strong>查看新咨询需求</strong><p>按状态筛选并安排跟进</p></div><Icon name="arrow" size={18}/></a>{pending===0&&<p className="muted">当前没有待审核资料。</p>}</div></section><section className="panel"><div className="section-head"><h2>发布前，核对三件事</h2><Icon name="shield"/></div><div className="checklist"><p><span>01</span><strong>内容有来源</strong><small>出处、版本与适用范围明确</small></p><p><span>02</span><strong>授权在有效期内</strong><small>公开字段与检索范围分别核验</small></p><p><span>03</span><strong>审批与发布分开</strong><small>草稿修改不影响当前线上版本</small></p></div></section></div><section className="panel"><div className="section-head"><h2>最近操作</h2><a className="text-link" href={url('/admin/audit')}>查看全部记录 <Icon name="arrow" size={16}/></a></div>{role==='admin'?<AuditTable rows={audit.slice(0,5)}/>:<p className="muted">完整操作记录仅项目管理员可查看。</p>}</section></>}

function AuditTable({rows}:{rows:ReturnType<typeof useApp>['audit']}){return <div className="table-wrap"><table><thead><tr><th>操作</th><th>对象</th><th>操作身份</th><th>时间</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td>{a.action}</td><td>{a.object}</td><td>{a.role}</td><td className="small muted">{a.time}</td></tr>)}</tbody></table>{!rows.length&&<p className="table-empty">暂无操作记录</p>}</div>}

export function Content(){const app=useApp();const [q,setQ]=useState(''),[filter,setFilter]=useState('all'),[create,setCreate]=useState(false),[name,setName]=useState(''),[category,setCategory]=useState<'绿茶'|'红茶'>('绿茶'),[revoke,setRevoke]=useState(false);const closeCreate=useCallback(()=>setCreate(false),[]),closeRevoke=useCallback(()=>setRevoke(false),[]);const canEdit=['operator','admin'].includes(app.role);const shown=app.items.filter(t=>(filter==='all'||(t.draftStatus||t.status)===filter)&&`${t.name} ${t.sku} ${t.batch}`.includes(q));const add=()=>{if(!name.trim())return;const id='draft-'+Date.now();app.setItems(a=>[...a,{...a[0],id,name:name.trim(),category,sku:'DEMO-'+Date.now().toString().slice(-5),batch:'NEW-DEMO',status:'draft',revision:1,draftStatus:undefined,offer:'unavailable',description:'新建的原型演示草稿。'}]);app.record('新建草稿',name);setCreate(false);goto('/admin/edit/'+id)};return <><div className="admin-title"><div><div className="eyebrow">CONTENT / 内容管理</div><h1>茶品与内容</h1><p>将茶品、资料与批次清晰关联。未经审核的内容不会进入用户端。</p></div><Button disabled={!canEdit} onClick={()=>setCreate(true)} icon="edit">新建草稿</Button></div><div className="panel"><div className="admin-table-tools"><div className="search-field"><Icon name="search"/><input aria-label="后台搜索茶品" placeholder="茶名、SKU、批次" value={q} onChange={e=>setQ(e.target.value)}/></div><select aria-label="筛选内容状态" value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">全部状态</option>{Object.entries(statusNames).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select><Button tone="secondary" href="/admin/imports" icon="upload">批量导入</Button></div><div className="table-wrap"><table><thead><tr><th>茶品 / 商品编号</th><th>茶类</th><th>当前批次</th><th>版本</th><th>状态</th><th>操作</th></tr></thead><tbody>{shown.map(t=><tr key={t.id}><td><strong>{t.name}</strong><small>{t.sku}</small></td><td>{t.category}</td><td>{t.batch}</td><td>v{t.revision}.0</td><td><StatusBadge status={t.draftStatus||t.status}/>{t.draftStatus&&t.status==='published'&&<small>线上 v{t.revision}.0 保持可用</small>}</td><td><div className="table-actions"><a href={url('/admin/edit/'+t.id)}>{canEdit?'编辑':'详情'}</a><a href={url('/admin/review/'+t.id)}>审核 / 发布</a>{t.status==='published'&&<a href={url('/tea/'+t.id)}>前台查看</a>}</div></td></tr>)}</tbody></table>{!shown.length&&<p className="table-empty">没有匹配的内容，试试其他筛选条件。</p>}</div><div className="table-footer">共 {shown.length} 条内容 <span>本地演示状态会保留；可在右上角重置。</span></div></div><div className="panel source-admin"><div><h3>示例来源授权</h3><p>撤回来源后，依赖此来源的公开茶品与问答依据会立即不可用。</p><Badge tone={app.sourceActive?'success':'danger'}>{app.sourceActive?'演示授权有效':'演示授权已撤回'}</Badge></div><Button tone={app.sourceActive?'danger':'secondary'} disabled={!['reviewer','admin'].includes(app.role)} onClick={()=>app.sourceActive?setRevoke(true):(app.setSourceActive(true),app.record('恢复示例来源','茶序示例资料'),app.toast('示例来源已恢复'))}>{app.sourceActive?'撤回示例来源授权':'恢复示例来源授权'}</Button></div>{!canEdit&&<Notice title="当前为只读或审核身份">修改内容请切换为数据运营；审核人与编辑身份分开演示。</Notice>}{create&&<Modal title="新建茶品草稿" onClose={closeCreate}><label className="field">茶品名称<input value={name} onChange={e=>setName(e.target.value)} maxLength={120} autoFocus/></label><label className="field">茶类<select value={category} onChange={e=>setCategory(e.target.value as '绿茶'|'红茶')}><option>绿茶</option><option>红茶</option></select></label><Notice title="新内容仅保存为草稿">还需补齐资料来源、批次和参数，再提交审核。</Notice><div className="modal-actions"><Button tone="secondary" onClick={closeCreate}>取消</Button><Button disabled={!name.trim()} onClick={add}>创建草稿</Button></div></Modal>}{revoke&&<Modal title="撤回这份示例来源？" onClose={closeRevoke}><p>相关公开茶品与问答依据将立即不可用。历史内容和操作记录仍会保留；演示中可再次恢复授权。</p><div className="modal-actions"><Button tone="secondary" onClick={closeRevoke}>取消</Button><Button tone="danger" onClick={()=>{app.setSourceActive(false);app.record('撤回来源授权','茶序示例资料');closeRevoke();app.toast('已撤回：相关公开内容不再可用')}}>确认撤回</Button></div></Modal>}</>}

export function EditContent({id}:{id:string}){const app=useApp();const item=app.items.find(t=>t.id===id);const [note,setNote]=useState(item?.draftNote||item?.description||''),[error,setError]=useState('');if(!item)return <Empty title="未找到内容" action={<Button href="/admin/content">返回内容列表</Button>}>请重新选择一条内容记录。</Empty>;const canEdit=['operator','admin'].includes(app.role);const status=item.draftStatus||item.status;const editable=canEdit&&!['pending_review','approved'].includes(status);const save=(submit=false)=>{if(!editable)return;if(note.trim().length<10){setError('请至少填写 10 个字的资料说明');return;}app.setItems(xs=>xs.map(x=>x.id===id?{...x,draftNote:note,...(x.status==='published'?{draftStatus:submit?'pending_review':'draft'}:{status:submit?'pending_review':'draft'})}:x));app.record(submit?'提交审核':'保存草稿',item.name);app.toast(submit?'已提交审核，请切换为客户审核人处理':'草稿已保存，公开版本未被覆盖');if(submit)goto('/admin/review/'+id)};return <><div className="breadcrumb"><a href={url('/admin/content')}><Icon name="back" size={16}/> 返回内容列表</a></div><div className="admin-title"><div><h1>{item.name}</h1><p>{item.sku} / {item.batch}</p></div><StatusBadge status={status}/></div><div className="admin-two-col wide-left"><section className="panel"><h2>内容资料</h2><div className="read-fields"><div><span>茶类</span><strong>{item.category}</strong></div><div><span>产地</span><strong>{item.origin}</strong></div><div><span>版本</span><strong>v{item.revision}.0{item.status==='published'?' · 线上':''}</strong></div></div><label className="field">特点与资料说明<textarea rows={7} value={note} onChange={e=>setNote(e.target.value)} disabled={!editable} aria-invalid={!!error}/>{error&&<small className="field-error">{error}</small>}</label><div className="read-fields"><div><span>参考投茶量</span><strong>{item.grams} g</strong></div><div><span>参考水温</span><strong>{item.water} °C</strong></div><div><span>每泡时长</span><strong>{item.seconds} 秒</strong></div></div><Notice title="冲泡参数与来源为当前演示资料">本轮演示可修改内容说明并走通审核流程；完整配方编辑由正式后台承接。</Notice><div className="form-bottom"><Button tone="secondary" disabled={!editable} onClick={()=>save()}>保存草稿</Button><Button disabled={!editable} onClick={()=>save(true)} icon="arrow">提交审核</Button></div></section><aside className="panel"><h2>版本与发布规则</h2><ol className="review-checks"><li>编辑已发布内容时，创建新的工作草稿。</li><li>新草稿不覆盖当前公开版本。</li><li>审核人确认内容后，单独执行发布。</li><li>发布后，用户端读取新的版本。</li></ol><Badge tone="warning">操作身份：{roleNames[app.role]}</Badge>{!canEdit&&<p className="muted">当前身份没有编辑权限，请切换数据运营。</p>}<a className="text-link mt24" href={url('/admin/review/'+id)}>查看审核与版本对比 <Icon name="arrow" size={16}/></a></aside></div></>}

export function Review({id}:{id:string}){const app=useApp();const item=app.items.find(t=>t.id===id);const [comment,setComment]=useState(''),[confirm,setConfirm]=useState(false);const close=useCallback(()=>setConfirm(false),[]);if(!item)return <Empty title="请选择一条待审核内容" action={<Button href="/admin/content">查看内容列表</Button>}>审核对象必须关联明确的内容和版本。</Empty>;const current=item.draftStatus||item.status;const canReview=app.role==='reviewer';const act=(action:'approve'|'reject'|'publish'|'withdraw')=>{if(action==='withdraw'?!['reviewer','admin'].includes(app.role):!canReview)return;if(action==='reject'&&!comment.trim()){app.toast('退回前请填写修改意见');return;}app.setItems(xs=>xs.map(x=>x.id!==id?x:action==='publish'?{...x,status:'published',draftStatus:undefined,description:x.draftNote||x.description,draftNote:undefined,revision:x.status==='published'?x.revision+1:x.revision}:action==='withdraw'?{...x,status:'withdrawn',draftStatus:undefined}:x.status==='published'?{...x,draftStatus:action==='approve'?'approved':'draft'}:{...x,status:action==='approve'?'approved':'draft'}));app.record(({approve:'审核通过',reject:'退回修改',publish:'发布版本',withdraw:'下线内容'})[action],item.name);app.toast(({approve:'审核已通过，尚未发布',reject:'已退回草稿，等待修改',publish:'已发布，用户端已更新',withdraw:'已下线，用户端不可见'})[action]);setConfirm(false)};return <><div className="breadcrumb"><a href={url('/admin/content')}><Icon name="back" size={16}/> 返回内容列表</a></div><div className="admin-title"><div><div className="eyebrow">REVIEW & PUBLISH / 审核发布</div><h1>{item.name} · 内容审核</h1><p>{item.batch} · 编辑与审批身份分离</p></div><StatusBadge status={current}/></div><div className="review-progress">{['草稿','提交审核','审核通过','正式发布'].map((t,i)=><div key={t} className={i<=({draft:0,pending_review:1,approved:2,published:3,withdrawn:0})[current]?'done':''}><span>{i+1}</span>{t}</div>)}</div><div className="admin-two-col wide-left"><section className="panel"><div className="section-head"><h2>版本对比</h2><Badge>{item.draftStatus?'线上版本与工作草稿':'首次版本核对'}</Badge></div><div className="diff-grid"><div><span className="eyebrow">{item.status==='published'?'当前公开版本':'原始资料'} · V{item.revision}.0</span><p>{item.description}</p></div><div className="diff-new"><span className="eyebrow">{item.draftStatus?'待发布工作版本':'本次审核内容'}</span><p>{item.draftNote||item.description}</p></div></div><h3 className="mt24">资料校验</h3><div className="validation-list"><p><Icon name="check"/> 茶名、SKU 和批次关联完整</p><p><Icon name="check"/> 使用演示来源，已明确标注</p><p className={!app.sourceActive?'text-danger':''}><Icon name={app.sourceActive?'check':'info'}/> {app.sourceActive?'来源授权状态有效':'来源已撤回，禁止发布'}</p></div><Notice tone="warning" title="这是原型审核流程，不是真实专家签审">正式内容需由客户指定审核人核对依据、口径和披露范围。</Notice></section><aside className="panel"><h2>审核意见</h2><label className="field">给资料维护人的说明<textarea rows={5} value={comment} onChange={e=>setComment(e.target.value)} placeholder="通过时可填写备注；退回时必须说明原因。" disabled={!canReview}/></label>{!canReview&&<Notice title="当前身份不能审批">请在顶部将演示身份切换为“客户审核人”。项目管理员不自动拥有专家审批权。</Notice>}<div className="vertical-buttons"><Button disabled={!canReview||current!=='pending_review'||!app.sourceActive} onClick={()=>act('approve')} icon="check">通过审核</Button><Button tone="secondary" disabled={!canReview||current!=='pending_review'} onClick={()=>act('reject')}>退回修改</Button><Button disabled={!canReview||current!=='approved'||!app.sourceActive} onClick={()=>act('publish')} icon="arrow">发布此版本</Button>{item.status==='published'&&<Button tone="danger" disabled={!['reviewer','admin'].includes(app.role)} onClick={()=>setConfirm(true)}>下线此内容</Button>}</div><p className="small muted">审核通过不会自动公开。发布成功后，用户端才会更新。</p></aside></div>{confirm&&<Modal title="确认下线这份内容？" onClose={close}><p>用户端将不再展示该茶品。版本和审计记录会保留，可重新整理草稿后审核发布。</p><div className="modal-actions"><Button tone="secondary" onClick={close}>取消</Button><Button tone="danger" onClick={()=>act('withdraw')}>确认下线</Button></div></Modal>}</>}

function parseCsv(text:string){const rows:string[][]=[];let row:string[]=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){cell+='"';i++;}else quoted=!quoted;}else if(c===','&&!quoted){row.push(cell);cell='';}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(cell);if(row.some(Boolean))rows.push(row);row=[];cell='';}else cell+=c;}row.push(cell);if(row.some(Boolean))rows.push(row);return rows;}
export function Imports(){const app=useApp();const [file,setFile]=useState(''),[state,setState]=useState('idle'),[rows,setRows]=useState<string[][]>([]),[errors,setErrors]=useState<{row:number;field:string;message:string}[]>([]),[header,setHeader]=useState<string[]>([]);const allowed=['operator','admin'].includes(app.role);const validate=(name:string,text:string)=>{setFile(name);setState('validating');const parsed=parseCsv(text.replace(/^\ufeff/,'')),head=parsed.shift()||[],errs:{row:number;field:string;message:string}[]=[];for(const key of ['name','category','sku','batch'])if(!head.includes(key))errs.push({row:1,field:key,message:'缺少必需列'});const seen=new Set(app.items.map(t=>t.sku+'|'+t.batch));if(parsed.length>5000)errs.push({row:1,field:'file',message:'超过 5000 行限制'});parsed.slice(0,5000).forEach((r,i)=>{for(const key of ['name','category','sku','batch'])if(!r[head.indexOf(key)]?.trim())errs.push({row:i+2,field:key,message:'必填内容缺失'});if(!['绿茶','红茶'].includes(r[head.indexOf('category')]))errs.push({row:i+2,field:'category',message:'仅支持本期范围：绿茶或红茶'});if(r.some(v=>/^[=+@\-]/.test(v)))errs.push({row:i+2,field:'row',message:'不接受公式或可执行单元格'});const key=r[head.indexOf('sku')]+'|'+r[head.indexOf('batch')];if(seen.has(key))errs.push({row:i+2,field:'sku,batch',message:'商品编号与批次重复'});seen.add(key)});setHeader(head);setRows(parsed);setErrors(errs);setTimeout(()=>setState(errs.length?'invalid':'validated'),500)};const load=async(f:File)=>{if(!allowed)return;if(f.size>10*1024*1024||!f.name.toLowerCase().endsWith('.csv')){setFile(f.name);setErrors([{row:0,field:'file',message:'请使用 10MB 以内的 CSV 文件'}]);setState('invalid');return;}validate(f.name,await f.text())};const commit=()=>{if(state!=='validated'||!allowed)return;app.setItems(xs=>[...xs,...rows.map((r,i)=>({...xs[0],id:'import-'+Date.now()+'-'+i,name:r[header.indexOf('name')],category:r[header.indexOf('category')] as TeaItem['category'],sku:r[header.indexOf('sku')],batch:r[header.indexOf('batch')],status:'draft' as const,draftStatus:undefined,revision:1,offer:'unavailable' as const,description:'CSV 导入的演示草稿。正式来源与配方需继续补齐。'}))]);setState('committed');app.record('导入草稿',file+' · '+rows.length+' 条');app.toast('已导入为草稿，尚未公开')};return <><div className="admin-title"><div><div className="eyebrow">DATA IMPORT / 导入中心</div><h1>先校验，再进入内容库</h1><p>导入只生成草稿；校验完成不会自动发布。</p></div><Button tone="secondary" icon="download" onClick={()=>downloadCsv('茶序_演示导入模板.csv',[['name','category','sku','batch'],['新茶品示例','绿茶','DEMO-NEW','2026-DEMO']])}>下载 CSV 模板</Button></div><div className="import-steps"><span className="active">01 上传资料</span><span className={state!=='idle'?'active':''}>02 校验字段</span><span className={state==='committed'?'active':''}>03 导入为草稿</span></div><section className="panel"><label className={'upload-area '+(!allowed?'disabled':'')}><Icon name="upload" size={38}/><strong>选择一份 CSV 资料文件</strong><p>最大 10MB · 最多 5000 行 · 文件保持 UTF-8 编码</p><span className="btn secondary">选择文件</span><input type="file" accept=".csv,text/csv" onChange={e=>{const f=e.target.files?.[0];if(f)void load(f);e.target.value=''}} disabled={!allowed} aria-label="上传CSV资料"/></label><div className="example-actions"><span className="small muted">也可以体验预置样例</span><button disabled={!allowed} className="text-link" onClick={()=>validate('错误样例.csv','name,category,sku,batch\n示例绿茶,绿茶,DEMO-ERR,2026-X\n,乌龙茶,,2026-Y')}>查看有错误的文件</button><button disabled={!allowed} className="text-link" onClick={()=>validate('正确样例.csv','name,category,sku,batch\n春日示例茶,绿茶,DEMO-'+Date.now()+',2026-X')}>查看可导入的文件</button></div></section>{state!=='idle'&&<section className="panel"><div className="section-head"><div><h2>{file}</h2><p className="muted small">{rows.length} 条数据 · {errors.length} 项错误</p></div><Badge tone={state==='invalid'?'danger':state==='validating'?'warning':'success'}>{{validating:'正在校验',invalid:'校验未通过',validated:'校验通过',committed:'已导入草稿'}[state]}</Badge></div>{state==='invalid'?<div className="table-wrap"><table><thead><tr><th>文件行号</th><th>字段</th><th>错误说明</th></tr></thead><tbody>{errors.map((e,i)=><tr key={i}><td>{e.row||'文件'}</td><td>{e.field}</td><td className="text-danger">{e.message}</td></tr>)}</tbody></table></div>:state==='committed'?<Notice title="已保存为草稿">请前往内容管理补齐来源与配方，审核发布后才能公开。</Notice>:<p className="muted">{state==='validating'?'正在检查必填项、重复标识与格式…':'所有示例行通过校验。可以将本批资料提交为草稿。'}</p>}<div className="form-bottom"><Button disabled={state!=='validated'||!rows.length||!allowed} onClick={commit} icon="arrow">导入为草稿</Button><Button href="/admin/content" tone="secondary">前往内容管理</Button></div></section>}<Notice title="原型支持 CSV 上传与行级校验">XLSX、文件存储与正式业务校验由后续后端实现。此处导入的是本机演示记录。</Notice></>}

export function Leads(){const app=useApp();const [filter,setFilter]=useState('all'),[selected,setSelected]=useState<string|null>(null),[status,setStatus]=useState(''),[note,setNote]=useState('');const can=['lead','admin'].includes(app.role),rows=app.leads.filter(l=>filter==='all'||l.status===filter),lead=app.leads.find(l=>l.id===selected);const labels:Record<string,string>={new:'待跟进',assigned:'已指派',contacted:'已联系',closed:'已关闭'};const close=useCallback(()=>setSelected(null),[]);const next:Record<string,string[]>={new:['new','assigned','contacted','closed'],assigned:['assigned','contacted','closed'],contacted:['contacted','closed'],closed:['closed']};if(!can)return <Empty title="当前身份没有线索查看权限" action={<Button href="/admin">返回工作概览</Button>}>请将演示身份切换为线索跟进或项目管理员。</Empty>;return <><div className="admin-title"><div><div className="eyebrow">INQUIRIES / 咨询线索</div><h1>让每一次咨询都有回应</h1><p>仅展示脱敏的演示数据。导出和跟进操作均记录到操作日志。</p></div><Button tone="secondary" icon="download" onClick={()=>{downloadCsv('茶序_脱敏演示线索.csv',[['编号','茶品','类型','联系方式（脱敏）','状态'],...rows.map(l=>[l.id,l.item,l.kind,l.contact,labels[l.status]])]);app.record('导出脱敏线索',rows.length+' 条');app.toast('已导出脱敏演示线索')}}>导出当前列表</Button></div><section className="panel"><div className="admin-table-tools"><div className="segmented">{[['all','全部'],['new','待跟进'],['contacted','已联系'],['closed','已关闭']].map(([k,t])=><button key={k} className={filter===k?'active':''} onClick={()=>setFilter(k)}>{t}</button>)}</div><span className="muted small">共 {rows.length} 条</span></div><div className="table-wrap"><table><thead><tr><th>需求编号 / 时间</th><th>茶品</th><th>类型</th><th>联系方式</th><th>状态</th><th>操作</th></tr></thead><tbody>{rows.map(l=><tr key={l.id}><td><strong>{l.id}</strong><small>{l.date}</small></td><td>{l.item}</td><td>{l.kind}</td><td>{l.contact}</td><td><Badge tone={l.status==='new'?'warning':'neutral'}>{labels[l.status]}</Badge></td><td><button className="text-link" onClick={()=>{setSelected(l.id);setStatus(l.status);setNote(l.note)}}>查看并跟进</button></td></tr>)}</tbody></table>{!rows.length&&<p className="table-empty">该状态下暂无咨询需求。</p>}</div></section>{lead&&<Modal title="需求详情与跟进" onClose={close}><Badge>{lead.kind}</Badge><h3>{lead.item}</h3><dl className="fact-list"><div><dt>需求编号</dt><dd>{lead.id}</dd></div><div><dt>联系方式</dt><dd>{lead.contact} · 已脱敏</dd></div><div><dt>同意记录</dt><dd>演示用途说明 v1 · 用户主动确认</dd></div></dl><label className="field">跟进状态<select value={status} onChange={e=>setStatus(e.target.value)}>{next[lead.status].map(s=><option value={s} key={s}>{labels[s]}</option>)}</select></label><label className="field">跟进备注<textarea rows={4} value={note} onChange={e=>setNote(e.target.value)}/></label><div className="modal-actions"><Button tone="secondary" onClick={close}>取消</Button><Button onClick={()=>{if(status==='closed'&&!note.trim()){app.toast('关闭前请填写跟进备注');return;}app.setLeads(ls=>ls.map(l=>l.id===lead.id?{...l,status:status as typeof l.status,note}:l));app.record('更新线索状态',lead.id+' → '+labels[status]);close();app.toast('跟进记录已保存')}}>保存跟进</Button></div></Modal>}</>}

export function Users(){const app=useApp();return <><div className="admin-title"><div><div className="eyebrow">ACCESS CONTROL / 账号与权限</div><h1>让合适的人，处理合适的内容</h1><p>使用右上角“演示身份”切换角色，验证不同操作的可见与可用状态。</p></div></div><Notice title="前端角色切换仅用于交互演示">正式系统必须由后端验证会话和权限。此原型不创建真实账号，也不实现生产身份认证。</Notice><section className="panel mt24"><div className="table-wrap"><table><thead><tr><th>演示身份</th><th>编辑与导入</th><th>内容审批</th><th>发布 / 下线</th><th>线索与导出</th><th>账号管理</th></tr></thead><tbody>{[['数据运营','可编辑 / 导入','不可审批','不可发布','无权限','无权限'],['客户审核人','只读','可审批','可发布 / 下线','无权限','无权限'],['线索跟进','无权限','无权限','无权限','可跟进 / 导出','无权限'],['项目管理员','可编辑 / 导入','不可专家审批','可下线，不可发布','可跟进 / 导出','管理身份']].map(r=><tr key={r[0]}>{r.map((c,i)=><td key={i}>{c}</td>)}</tr>)}</tbody></table></div></section><div className="admin-two-col mt24"><section className="panel"><Icon name="shield" size={28}/><h2>最少必要权限</h2><p className="muted">管理员不自动拥有专家审核权限。修改草稿后，切换审核身份才能批准和发布。</p><Badge>当前身份：{roleNames[app.role]}</Badge></section><section className="panel"><Icon name="eye" size={28}/><h2>敏感信息边界</h2><p className="muted">供应商联系人、采购价和真实客户联系方式不进入本原型公开页面。演示线索导出也保持脱敏。</p><a className="text-link" href={url('/admin/audit')}>查看操作记录 <Icon name="arrow" size={16}/></a></section></div></>}

export function AuditPage(){const {audit,role}=useApp();const [q,setQ]=useState('');if(role!=='admin')return <Empty title="操作日志仅管理员可查看">请切换为项目管理员体验只读审计界面。</Empty>;return <><div className="admin-title"><div><div className="eyebrow">AUDIT LOG / 操作记录</div><h1>关键变更，有迹可循</h1><p>原型记录当前设备上的演示操作；不提供删除日志入口。</p></div></div><section className="panel"><div className="search-field"><Icon name="search"/><input aria-label="筛选操作日志" value={q} onChange={e=>setQ(e.target.value)} placeholder="按操作或对象关键词筛选"/></div><AuditTable rows={audit.filter(a=>(a.action+a.object).includes(q))}/></section></>}
